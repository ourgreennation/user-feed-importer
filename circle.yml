machine:
  php:
    version: 5.6.17

dependencies:
  pre:
    - composer global require wp-coding-standards/wpcs
    - sleep 10
    - phpcs --config-set installed_paths $HOME/.composer/vendor/wp-coding-standards/wpcs
    - phpcs -i
test:
  override:
    - phpcs --standard=phpcs.ruleset.xml src/
  post:
    - chmod +x ./circleci/auto-phpcbf.sh && ./circleci/auto-phpcbf.sh
    - rm -rf .git

deployment:
  deploy:
    branch: master
    commands:
      - rsync -avz -e 'ssh -p 18765' --progress ./ leyan503@77.104.152.91:/home/leyan503/public_html/wp-content/plugins/user-feed-importer  --delete
